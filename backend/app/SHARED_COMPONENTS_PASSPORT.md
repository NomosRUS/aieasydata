# ОБЩИЕ КОМПОНЕНТЫ СИСТЕМЫ - ПАСПОРТ ДЛЯ LLM

## 🎯 СТАТУС: ПОЛНОСТЬЮ ФУНКЦИОНАЛЬНЫЕ ОБЩИЕ КОМПОНЕНТЫ

**Назначение:** Общие компоненты и утилиты, используемые всеми модулями системы AiEasyData для обеспечения единообразия, переиспользования кода и централизованного управления.

**Статус:** ✅ ПОЛНОСТЬЮ ФУНКЦИОНАЛЬНЫ И ИНТЕГРИРОВАНЫ

Общие компоненты обеспечивают базовую функциональность для всех 5 модулей системы и содержат критически важные утилиты для профилирования данных, ИИ-анализа, генерации DAG'ов и управления базой данных.

## 📊 АРХИТЕКТУРА ОБЩИХ КОМПОНЕНТОВ

### 🔧 ОСНОВНЫЕ КОМПОНЕНТЫ

#### **1. settings.py - Конфигурация системы**
```python
class OpenAISettings(BaseSettings):
    api_key: str = Field(..., min_length=1)
    base_url: str = "https://api.openai.com/v1"
    model: str = "gpt-4o-mini"

class LocalLLMSettings(BaseSettings):
    base_url: str = "http://ollama:11434"
    model: str = "gemma:2b"
    api_key: str = "ollama"
```

**Функции:**
- ✅ **Настройки OpenAI** - API ключ, базовый URL, модель
- ✅ **Настройки локальной LLM** - Ollama интеграция
- ✅ **Централизованная конфигурация** для всех модулей
- ✅ **Переменные окружения** с префиксами OPENAI_ и LOCAL_LLM_

#### **2. database.py - Управление базой данных**
```python
class DataProfile(Base):
    __tablename__ = "data_profiles"
    
    id = Column(Integer, primary_key=True, index=True)
    source_path = Column(String, unique=True, index=True, nullable=False)
    kind = Column(String, nullable=True)
    total_row_count = Column(BigInteger, nullable=True)
    file_count = Column(Integer, nullable=True)
    columns = Column(JSONB, nullable=True)
    sample_data = Column(JSONB, nullable=True)
    llm_summary = Column(Text, nullable=True)
    error = Column(Text, nullable=True)
```

**Функции:**
- ✅ **SQLAlchemy ORM** - подключение к PostgreSQL
- ✅ **Модель DataProfile** - профили данных для всех источников
- ✅ **Upsert операции** - вставка/обновление профилей
- ✅ **Сессии БД** - SessionLocal для всех модулей
- ✅ **JSONB поддержка** - хранение сложных структур данных

#### **3. profiler.py - Профилирование данных**
```python
def scan_data_landing_zone(path: str) -> List[str]:
    """Recursively scans the data landing zone and returns supported files."""
    
def profile_source(source_path: str) -> Dict[str, Any]:
    """Профилирует источник данных (папку с файлами)."""
    
def classify_table(profile_data: Dict[str, Any]) -> str:
    """Classifies a table as 'fact', 'dimension', or 'mapping'."""
```

**Поддерживаемые форматы:**
- ✅ **CSV, JSON, JSONL** - с автоопределением разделителей
- ✅ **XML** - с многопроцессорной обработкой и таймаутами
- ✅ **Parquet, XLSX, TSV** - прямая поддержка
- ✅ **Большие файлы** - оптимизированная обработка с таймаутами

**Ключевые возможности:**
- ✅ **Сканирование data_landing_zone** - рекурсивный поиск файлов
- ✅ **Профилирование источников** - анализ структуры и содержимого
- ✅ **Классификация таблиц** - fact/dimension/mapping на основе эвристик
- ✅ **Подсчет строк** - оптимизированный для разных форматов
- ✅ **Многопроцессорность** - для тяжелых XML файлов

#### **4. analyzer.py - Анализ данных**
```python
def quick_profile(cfg: dict):
    """Real CSV profiler that analyzes actual file structure."""
```

**Функции:**
- ✅ **Быстрое профилирование** - анализ структуры CSV файлов
- ✅ **Определение типов данных** - Int64, Float64, DateTime, String
- ✅ **Обработка путей** - конвертация контейнерных путей в локальные
- ✅ **Анализ временных полей** - автоопределение дат
- ✅ **Образцы данных** - извлечение примеров для анализа

#### **5. agent.py - ИИ-агент**
```python
def get_llm_response(prompt: str) -> dict:
    """Centralized function to interact with the OpenAI LLM."""
    
def _client():
    """Creates OpenAI client with proper configuration."""
```

**Функции:**
- ✅ **Централизованный ИИ-клиент** - единая точка доступа к LLM
- ✅ **OpenAI интеграция** - поддержка API ключей и базовых URL
- ✅ **Обработка ошибок** - graceful degradation при сбоях
- ✅ **Отладочные логи** - детальная информация о запросах
- ✅ **Jinja2 шаблоны** - рендеринг промптов из шаблонов

#### **6. rule_engine.py - Движок правил**
```python
def recommend(profile: dict):
    """Recommends storage system based on data profile."""
```

**Логика рекомендаций:**
- ✅ **ClickHouse** - для больших данных (>1M строк) с временными полями
- ✅ **PostgreSQL** - для небольших данных (<1M строк) без времени
- ✅ **HDFS** - для сырых/разнородных данных или архивов
- ✅ **Эвристики** - на основе размера данных и наличия временных полей

#### **7. dag_compiler.py - Компилятор DAG'ов**
```python
def compile_dag(dsl: dict) -> str:
    """Compiles DSL to Airflow DAG using Jinja2 templates."""
```

**Функции:**
- ✅ **Jinja2 шаблоны** - генерация DAG'ов из DSL
- ✅ **Warehouse ETL DAG** - специальные шаблоны для хранилищ
- ✅ **Автоматическое сохранение** - в директорию Airflow DAG'ов
- ✅ **Гибкая конфигурация** - поддержка различных типов DAG'ов

#### **8. crud.py - CRUD операции**
```python
def get_connection(db: Session, connection_id: int)
def create_connection(db: Session, connection: schemas.ConnectionCreate)
def get_connections(db: Session, skip: int = 0, limit: int = 100)
```

**Функции:**
- ✅ **Управление подключениями** - CRUD для источников данных
- ✅ **SQLAlchemy интеграция** - типизированные операции с БД
- ✅ **Пагинация** - поддержка skip/limit для больших списков

#### **9. schemas.py - Схемы данных**
```python
class ConnectionBase(BaseModel):
    name: str
    type: Literal["postgres", "clickhouse", "hdfs", "s3", "csv", "json", "xml"]
    uri: str
```

**Функции:**
- ✅ **Pydantic модели** - типизированные схемы для API
- ✅ **Поддержка типов подключений** - все основные источники данных
- ✅ **Валидация данных** - автоматическая проверка входных данных

#### **10. main.py - Главное приложение**
```python
app = FastAPI(title="AiEasyData API (OpenAI)", version="0.1.0")

# Подключение всех модулей
app.include_router(warehouse_router)      # Модуль 4
app.include_router(metrics_router)        # Модуль 5  
app.include_router(performance_router)    # Модуль 3
app.include_router(aggregation_router)    # Модуль 2
app.include_router(data_validator_router) # Модуль 1
```

**Функции:**
- ✅ **FastAPI приложение** - центральная точка входа
- ✅ **CORS middleware** - поддержка кросс-доменных запросов
- ✅ **Интеграция всех модулей** - единое API для всех 5 модулей
- ✅ **DSL модели** - для создания ETL пайплайнов
- ✅ **Профили данных API** - endpoints для работы с профилями

#### **11. shared/base_profiler.py - Базовое профилирование**
```python
def scan_data_landing_zone(path: str) -> List[str]:
    """Рекурсивно сканирует зону данных и возвращает список файлов."""
    
def get_basic_file_info(file_path: str) -> Dict[str, Any]:
    """Получает базовую информацию о файле: размер, тип, количество строк."""
    
def get_basic_schema_info(file_path: str, sample_size: int = 100) -> Dict[str, Any]:
    """Получает базовую информацию о схеме: колонки, типы, примеры."""
```

**Функции:**
- ✅ **Базовое сканирование** - поиск поддерживаемых файлов
- ✅ **Информация о файлах** - размер, тип, количество строк
- ✅ **Схема данных** - колонки, типы, образцы данных
- ✅ **Переиспользование** - общий компонент для всех модулей

## 🔗 ИНТЕГРАЦИЯ С МОДУЛЯМИ

### **Модуль 1 (Валидация данных):**
- ✅ **shared/base_profiler.py** - базовое профилирование файлов
- ✅ **database.py** - для сохранения результатов валидации
- ✅ **rule_engine.py** - базовые рекомендации по СУБД
- ✅ **Специализация:** Оценка качества данных, поиск аномалий, очистка

### **Модуль 2 (Агрегация данных):**
- ✅ **shared/base_profiler.py** - профилирование источников
- ✅ **database.py** - для метаданных агрегации
- ✅ **rule_engine.py** - базовые рекомендации по хранению
- ✅ **Специализация:** Кастомные наборы данных, агрегации

### **Модуль 3 (Оптимизация производительности):**
- ✅ **shared/base_profiler.py** - базовое профилирование
- ✅ **rule_engine.py** - базовые рекомендации по СУБД
- ✅ **agent.py** - для ИИ-анализа производительности
- ✅ **database.py** - для сохранения рекомендаций
- ✅ **Специализация:** Анализ производительности, оптимизация запросов

### **Модуль 4 (Проектирование хранилищ):**
- ✅ **analyzer.py** - для анализа данных
- ✅ **rule_engine.py** - для выбора оптимальной СУБД
- ✅ **dag_compiler.py** - для генерации ETL DAG'ов
- ✅ **agent.py** - для ИИ-анализа требований
- ✅ **Специализация:** Проектирование схем, генерация DDL

### **Модуль 5 (Мониторинг хранилищ):**
- ✅ **data_type_detector.py** - ПЕРЕМЕЩЕН в модуль 5
- ✅ **shared/base_profiler.py** - базовое профилирование
- ✅ **database.py** - для сохранения метрик
- ✅ **Специализация:** Автоопределение типов, сбор метрик производительности

## 📋 API ENDPOINTS В MAIN.PY

### **Профилирование данных:**
```python
@app.post("/profile")
async def profile_data_source(request: ProfileRequest)

@app.get("/profiles", response_model=List[DataProfileResponse])
async def get_profiles(skip: int = 0, limit: int = 100)

@app.get("/profiles/{profile_id}", response_model=DataProfileResponse)  
async def get_profile(profile_id: int)
```

### **Генерация DAG'ов:**
```python
@app.post("/compile")
async def compile_dsl_to_dag(dsl: DSLModel)
```

### **ИИ-анализ:**
```python
@app.post("/ai-analyze")
async def ai_analyze_data(request: AIAnalysisRequest)
```

## 🎯 КЛЮЧЕВЫЕ ВОЗМОЖНОСТИ

### **1. Профилирование данных:**
- ✅ **Автоматическое сканирование** data_landing_zone
- ✅ **Поддержка 7+ форматов** файлов
- ✅ **Классификация таблиц** - fact/dimension/mapping
- ✅ **Оптимизированная обработка** больших файлов
- ✅ **Многопроцессорность** для XML

### **2. ИИ-интеграция:**
- ✅ **Централизованный клиент** OpenAI
- ✅ **Поддержка локальных LLM** (Ollama)
- ✅ **Jinja2 шаблоны** для промптов
- ✅ **Обработка ошибок** и отладка

### **3. Управление данными:**
- ✅ **SQLAlchemy ORM** для PostgreSQL
- ✅ **JSONB поддержка** для сложных структур
- ✅ **Upsert операции** для профилей
- ✅ **Типизированные CRUD** операции

### **4. Генерация пайплайнов:**
- ✅ **DSL → Airflow DAG** компиляция
- ✅ **Jinja2 шаблоны** для гибкости
- ✅ **Warehouse ETL** специализация
- ✅ **Автоматическое сохранение** DAG'ов

### **5. Рекомендательная система:**
- ✅ **Эвристики выбора СУБД** на основе данных
- ✅ **ClickHouse** для больших временных рядов
- ✅ **PostgreSQL** для транзакционных данных
- ✅ **HDFS** для сырых/архивных данных

## 🚀 АРХИТЕКТУРНЫЕ ПРЕИМУЩЕСТВА

### **1. Переиспользование кода:**
- Все модули используют общие компоненты
- Единообразие в работе с данными
- Централизованная конфигурация

### **2. Масштабируемость:**
- Модульная архитектура
- Независимые компоненты
- Легкое добавление новых модулей

### **3. Надежность:**
- Обработка ошибок на всех уровнях
- Graceful degradation
- Детальное логирование

### **4. Производительность:**
- Оптимизированная обработка больших файлов
- Многопроцессорность для тяжелых операций
- Кэширование и переиспользование соединений

## 📊 СТАТИСТИКА КОМПОНЕНТОВ

**Общий объем кода:** ~1,500+ строк
**Количество файлов:** 12 файлов
**Поддерживаемые форматы:** 7+ форматов данных
**API endpoints:** 10+ общих endpoints
**Интеграции:** 5 модулей + Airflow + PostgreSQL

## 🔍 АНАЛИЗ ПЕРЕСЕЧЕНИЙ С МОДУЛЯМИ

### **❗ НАЙДЕННЫЕ ДУБЛИРОВАНИЯ И ПЕРЕСЕЧЕНИЯ:**

#### **1. Профилирование данных:**
- **Общие:** `shared/base_profiler.py` - базовое профилирование (размер, тип, схема)
- **Модуль 1:** `QualityAssessor` - специализация на качестве данных
- **Модуль 3:** `PerformanceProfiler` - специализация на производительности
- **Модуль 5:** `MetricsCollector` - специализация на сборе метрик
- **✅ РАЗДЕЛЕНО:** Четкая специализация каждого компонента

#### **2. Автоопределение типов данных:**
- **Общие:** ~~`data_type_detector.py`~~ - ПЕРЕМЕЩЕН в модуль 5
- **Модуль 5:** `data_type_detector.py` - автоопределение типов
- **✅ ИСПРАВЛЕНО:** Дублирование устранено, компонент перемещен в модуль 5

#### **3. Рекомендации по СУБД:**
- **Общие:** `rule_engine.py` - базовые рекомендации по выбору СУБД
- **Модуль 1:** `StorageRecommender` - специализация на качестве данных
- **Модуль 3:** `OptimizationGenerator` - специализация на производительности
- **✅ СПЕЦИАЛИЗИРОВАНО:** Каждый компонент имеет свою область ответственности

### **✅ ПРАВИЛЬНЫЕ ИНТЕГРАЦИИ:**

#### **1. ИИ-анализ:**
- **Общие:** `agent.py` - централизованный ИИ-клиент
- **Модуль 4:** Использует `agent.py` для ИИ-анализа требований
- **✅ ПРАВИЛЬНО:** Единый ИИ-агент для всех модулей

#### **2. Анализ данных:**
- **Общие:** `analyzer.py` - быстрое профилирование CSV
- **Модуль 4:** Использует `analyzer.py` для анализа структуры данных
- **✅ ПРАВИЛЬНО:** Переиспользование общего компонента

#### **3. Управление БД:**
- **Общие:** `database.py` - модели и сессии
- **Все модули:** Используют общие модели и подключения
- **✅ ОТЛИЧНО:** Централизованное управление данными

### **🎯 ВЫПОЛНЕННЫЙ РЕФАКТОРИНГ:**

#### **1. ✅ УСТРАНЕНО дублирование автоопределения:**
```bash
# ВЫПОЛНЕНО: data_type_detector.py → модуль 5
mv backend/app/data_type_detector.py backend/app/metrics_collector/
```

#### **2. ✅ УНИФИЦИРОВАНЫ рекомендательные системы:**
- **✅ Оставлен:** `rule_engine.py` для базовых эвристик
- **✅ Модуль 1:** Специализация на качестве данных
- **✅ Модуль 3:** Специализация на производительности

#### **3. ✅ ЧЕТКО РАЗДЕЛЕНО профилирование:**
- **✅ shared/base_profiler.py:** Базовое сканирование и схема
- **✅ Модуль 1:** Валидация и качество
- **✅ Модуль 3:** Анализ производительности
- **✅ Модуль 5:** Мониторинг и метрики

## ✅ ЗАКЛЮЧЕНИЕ

**ОБЩИЕ КОМПОНЕНТЫ ОБЕСПЕЧИВАЮТ МОЩНУЮ ОСНОВУ ДЛЯ ВСЕЙ СИСТЕМЫ!**

Общие компоненты в папке `backend/app/` представляют собой критически важную инфраструктуру, которая:

- 🎯 **Обеспечивает единообразие** всех 5 модулей системы
- 🎯 **Предоставляет мощные утилиты** для профилирования и анализа данных
- 🎯 **Интегрирует ИИ-возможности** через централизованный агент
- 🎯 **Управляет жизненным циклом данных** от профилирования до DAG'ов
- 🎯 **Обеспечивает производительность** через оптимизированную обработку

**Эти компоненты являются фундаментом, на котором построены все специализированные модули системы AiEasyData!**

## 🎯 СТАТУС ЗАВЕРШЕНИЯ

**📅 ДАТА СОЗДАНИЯ ПАСПОРТА:** 27 сентября 2025  
**📁 РАСПОЛОЖЕНИЕ:** backend/app/ (корневая папка приложения)

**🎯 ПРОАНАЛИЗИРОВАННЫЕ ФАЙЛЫ:**
- ✅ `settings.py` - конфигурация системы
- ✅ `database.py` - управление БД и модели
- ✅ `profiler.py` - профилирование данных (215 строк)
- ✅ `analyzer.py` - анализ данных (90 строк)
- ✅ `agent.py` - ИИ-агент (95 строк)
- ✅ `rule_engine.py` - движок правил (21 строка)
- ✅ `dag_compiler.py` - компилятор DAG'ов (25 строк)
- ✅ `crud.py` - CRUD операции (19 строк)
- ✅ `schemas.py` - схемы данных (20 строк)
- ✅ `main.py` - главное приложение (217 строк)
- ✅ `data_type_detector.py` - детектор типов данных

**СТАТУС:** 🚀 ВСЕ ОБЩИЕ КОМПОНЕНТЫ ПОЛНОСТЬЮ ДОКУМЕНТИРОВАНЫ И ФУНКЦИОНАЛЬНЫ!
